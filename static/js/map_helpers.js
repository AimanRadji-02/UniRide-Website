/**
 * Maps are powered by the Google Maps JavaScript API (see templates and GOOGLE_MAPS_API_KEY).
 * This file is kept for compatibility; map logic lives in the relevant templates.
 */
function unirideMapsConfigured() {
  return !!(window.google && window.google.maps);
}
